const nome ='aline'; //string
const nome1 ="aline"; //string
const nome2 =`aline`; //string
const num1 = 10; //number
const num2 =10.53 //number
let nomeAluno; //undefineds -> não aponta pra local nenhum na memória
let sobrenomeAluno = null; //nulo -> não aponta pra local nenhum na memória
const aprovado = true; //Valor boolean = true ou false (lógico) desvio condicional
 
console.log(typeof nomeAluno, num2);

//Existe alguns dados primitivos sendo eles String, number, undefined, null, boolean e symbol(symbol é mais avançado será mencionado mais pra frente

let a = 123;
const b = a

console.log(a, b);

